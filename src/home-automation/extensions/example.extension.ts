import { TServiceParams } from "@digital-alchemy/core";

export function Example({ hass, lifecycle }: TServiceParams) {
  // const entity = hass.refBy.id("sensor.downstairs_temperature");
  // const toggles = hass.refBy.id("switch.desk_strip_office_plants");
  // hass.call.ecobee.create_vacation({
  //   cool_temp: 70,
  //   entity_id: "climate.downstairs",
  //   heat_temp: 60,
  //   vacation_name: "Generic Vacation",
  // });
  // lifecycle.onReady(() => {
  //   const sensor = hass.refBy.id("sensor.transmission_download_speed");
  //   sensor.onUpdate((new_state, old_state) => {
  //     console.log({ new_state, old_state });
  //   });
  // });
}
